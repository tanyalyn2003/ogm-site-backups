---
description: OGM knowledge base — read site structure and agent workflow docs before live-site tasks; never use backup folders as edit source
alwaysApply: true
---

# OGM Knowledge Base (agents)

Before **any** Olive Glass & Marble quoter-tool task (edits, uploads, debugging live files):

1. Read `/Users/tanyawhite/OGM/github-backups/ogm-workspace/docs/OGM-AGENT-WORKFLOW.md`
2. Read `/Users/tanyawhite/OGM/github-backups/ogm-workspace/docs/OGM-SITE-STRUCTURE.md`
3. Follow `.cursor/rules/ogm-edit-workflow.mdc` (fresh `start`, `finish`, explicit upload approval)

## Single GitHub repo

- **ONLY repo:** [`tanyalyn2003/ogm-site-backups`](https://github.com/tanyalyn2003/ogm-site-backups) — local clone at `/Users/tanyawhite/OGM/github-backups/`
- **Workspace** `/Users/tanyawhite/OGM/` is the working directory; it is **not** a GitHub push target
- **Do NOT** create, push to, or mention `tanyalyn2003/ogm`
- **Docs/rules/scripts:** edit under `ogm-workspace/` inside `github-backups/`, commit and push from `github-backups/` only
- **Live site files:** GoDaddy via `ogm-workflow.sh` — not stored in GitHub by default

## Never use as edit source

- Mac `backups/`, Archive folders, Trash
- Stale `quoter-tool-working/` without same-session `ogm-workflow.sh start`
- `fresh-godaddy-pulls/` except the pull just created by `start`
- GoDaddy server `backups/` folder (history only)
- GitHub repo (docs/scripts only — not live site files)

## Valid edit path

`ogm-workflow.sh start` → edit `quoter-tool-working/<file>` → `finish` → ask → optional `upload`

## More docs

Index: `github-backups/ogm-workspace/docs/README.md`

Changelog: `github-backups/ogm-workspace/docs/OGM-CHANGELOG.md`

Disk / prune: `github-backups/ogm-workspace/docs/OGM-LOCAL-DISK-PLAN.md`

Skill: `ogm-godaddy-workflow`
